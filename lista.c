#include<stdio.h>
#include<stdlib.h>

/*
char *obterIdLista(LIS_tppLista pLista) {
   #ifdef _DEBUG
   assert(plista != NULL) 
    #endif
    if(pLista == NULL) {
        return NULL;
    }
    return pLista->idLista;
   }

*/
